
class Topic:
    INSUFFICIENT_INGREDIENTS = 'insufficient_ingredients_notification'


def subscribe_listeners():
    """subscribe listeners here"""
    # pub.subscribe(subscriber1, Topic.INSUFFICIENT_INGREDIENTS)
    pass
